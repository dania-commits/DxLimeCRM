function addTask(){const i=document.getElementById('taskInput');if(i.value.trim()){const li=document.createElement('li');li.textContent=i.value;document.getElementById('timeline').appendChild(li);i.value='';}}
function extractInsights(){const notes=document.getElementById('notes').value.split('\n');const out=document.getElementById('insights');out.innerHTML='';notes.forEach(n=>{if(n.trim().startsWith('-')){const li=document.createElement('li');li.textContent=n.replace('-','').trim();out.appendChild(li);}});}
function analyze(){const p=document.getElementById('processInput').value;if(!p.trim())return;document.getElementById('analysis').textContent='Suggested CRM Improvement: Automate "'+p+'" within Lime CRM, define triggers, and align user workflows for higher adoption.';}